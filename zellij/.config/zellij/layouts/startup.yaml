---
session:
  name: "startup"
  attach: true

template:
  direction: Horizontal
  parts:
    - direction: Vertical
      borderless: true
      body: true

tabs:
  - direction: Vertical
    name: Startup
    run:
      command: {cmd: bash, args: ["-c", "sudo -H service uwsgi restart searx && topgrade"]}
    borderless: true
