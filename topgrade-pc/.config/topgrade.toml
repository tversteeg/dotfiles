no_retry = true
skip_notify = true
remote_topgrades = ["cems.local"]

[git]
repos = ["~/.dotfiles", "~/r/*", "~/w/*/*", "~/gh/*/*"]

[commands]
"Neovim Nightly" = "bob install nightly"
"i3status" = "cd ~/r/i3status-rust/ && cargo install --path . --locked && ./install.sh"
"helix" = "cd ~/r/helix/ && cargo install --path helix-term --locked"

[firmware]
upgrade = true
