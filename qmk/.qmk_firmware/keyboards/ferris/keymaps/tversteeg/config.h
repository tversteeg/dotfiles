#pragma once

// The tap dance time between the triggers
#define TAPPING_TERM 200

// Optimize layers, only allows up to 8 layers
#define LAYER_STATE_8BIT

// Allow rolling keys
#define IGNORE_MOD_TAP_INTERRUPT
